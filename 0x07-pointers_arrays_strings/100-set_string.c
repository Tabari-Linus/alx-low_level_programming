#include "main.h"

/**
 * set_string - sets value of a pointer to a char
 * @s: double pointer to a string
 * @x: pointer tp the string to set s to
 * Return: Description of the returned value
 */
void set_string(char **s, char *x)
{
*s = x;
}
